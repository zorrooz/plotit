% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compose.R
\name{compose_grid}
\alias{compose_grid}
\title{Assemble multiple plots into a grid layout}
\usage{
compose_grid(
  ...,
  ncol = NULL,
  nrow = NULL,
  byrow = TRUE,
  widths = NULL,
  heights = NULL,
  guides = NULL,
  axes = "keep",
  tag_levels = NULL
)
}
\arguments{
\item{...}{\code{plotit} or \code{plotit_composite} objects to arrange.}

\item{ncol}{Number of columns. \code{NULL} (default) = auto; if both \code{ncol}
and \code{nrow} are \code{NULL} defaults to 1 (vertical stack).}

\item{nrow}{Number of rows. \code{NULL} (default) = inferred from \code{ncol} and
the number of plots.}

\item{byrow}{Fill direction: \code{TRUE} (default) = row-major.}

\item{widths}{Relative column widths, e.g. \code{c(1, 2)}.}

\item{heights}{Relative row heights.}

\item{guides}{\code{"collect"} to merge legends, \code{"keep"} to separate,
\code{NULL} (default) for patchwork auto-detect.}

\item{axes}{\code{"collect"} to share all axes, \code{"collect_x"} or \code{"collect_y"}
for a single direction, \code{"keep"} (default) to keep axes independent.}

\item{tag_levels}{Sub-figure tag scheme: \code{"A"} for uppercase letters,
\code{"a"} for lowercase, \code{"1"} for numbers, \code{"i"} for roman numerals, or a
custom character vector (e.g. \code{c("(a)", "(b)")}).  \code{NULL} = no tags.}
}
\value{
A \code{plotit_composite} object.  Pipe it to \code{label_title()},
\code{style()}, or \code{export()}.
}
\description{
\code{compose_grid()} arranges \code{plotit} or \code{plotit_composite} objects into a
grid via \code{patchwork::wrap_plots()}.  The default vertical stack (\code{ncol = 1} when neither \code{ncol} nor \code{nrow} is given) is the most common layout for
report figures.  Pipe the result to \code{label_title()} / \code{style()} /
\code{export()} just as with a single \code{plotit}.
}
