% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compose.R
\name{compose_inset}
\alias{compose_inset}
\title{Overlay an inset plot on a base plot}
\usage{
compose_inset(
  base,
  inset,
  left = 0,
  bottom = 0,
  right = 1,
  top = 1,
  align_to = "panel",
  on_top = TRUE,
  ...
)
}
\arguments{
\item{base}{A \code{plotit} object serving as the background.}

\item{inset}{A \code{plotit} or \code{plotit_composite} object to overlay.}

\item{left, right, bottom, top}{Inset edges in npc (0-1).}

\item{align_to}{Coordinate reference: \code{"panel"} (default) or \code{"plot"}.}

\item{on_top}{Logical; \code{TRUE} (default) = inset rendered above base.}

\item{...}{Passed through to \code{patchwork::inset_element()}.}
}
\value{
A \code{plotit_composite} object.
}
\description{
Places a smaller plot (\code{inset}) as a floating panel on top of a base
\code{plotit} via \code{patchwork::inset_element()}.  Position is specified in
normalised parent coordinates (0-1 relative to the panel or plot area).
The returned composite accepts \code{label_title()} / \code{style()} / \code{export()}
in the usual way.
}
