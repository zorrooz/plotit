% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_rug}
\alias{mark_rug}
\title{Rug / tick layer}
\usage{
mark_rug(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  sides = "bl",
  length = NULL,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_rug}}

\item{sides}{Which sides to draw on: any combination of \code{"b"} (bottom),
\code{"l"} (left), \code{"t"} (top), \code{"r"} (right).  Default \code{"bl"}.}

\item{length}{Tick length as a fraction of the panel (default 0.03).}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Adds marginal tick marks along the axes: one short segment per
observation.  Use for 1D marginals under a histogram or density,
censoring ticks in survival timelines, or exact data positions
behind a smoothed curve.
}
\examples{
plotit(faithful, encode(x = eruptions)) |>
  mark_histogram(bins = 20) |>
  mark_rug()

# top rug to frame a density
plotit(faithful, encode(x = eruptions)) |>
  mark_density() |>
  mark_rug(sides = "t", color = "#E15759")

# two 1D marginals beside a scatter
plotit(iris, encode(x = Sepal.Width, y = Sepal.Length)) |>
  mark_point(alpha = 0.5) |>
  mark_rug(sides = "bl")
}
\references{
Vega-Lite: \href{https://vega.github.io/vega-lite/docs/rule.html}{Rule} (marginal tick pattern)

AntV G2: \href{https://g2.antv.antgroup.com/en/api/mark/range}{Range} (brush ticks)
}
