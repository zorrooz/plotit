% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_ecdf}
\alias{mark_ecdf}
\title{Empirical CDF layer}
\usage{
mark_ecdf(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  n = 1000,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics (\code{x} is the sample)}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to the underlying step layer}

\item{n}{Oversampling factor for the step function (default 1000;
use \code{Inf} for the exact step function).}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Plots the empirical cumulative distribution function as a stair step.
A distribution view with no binning parameter to choose: every point is
exactly represented, and group comparisons (quantiles, shifts, tails)
are easy to read.
}
\examples{
plotit(faithful, encode(x = eruptions)) |> mark_ecdf()

# ECDF comparison
df <- data.frame(
  value = c(iris$Sepal.Length, iris$Petal.Length),
  part = rep(c("Sepal", "Petal"), each = 150)
)
plotit(df, encode(x = value, colour = part)) |>
  mark_ecdf()
}
\references{
R: \code{stats::ecdf()} (empirical cumulative distribution)
Observable Plot: \code{Plot.ecdf}

Vega-Lite: \code{line}/\code{step} with cumulative \code{window} transform
}
