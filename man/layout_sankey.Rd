% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{layout_sankey}
\alias{layout_sankey}
\title{Sankey layout}
\usage{
layout_sankey(
  plot,
  node_width = 0.04,
  padding = 0.02,
  curvature = 0.5,
  n_points = 50,
  max_sweeps = 4L
)
}
\arguments{
\item{plot}{A \code{plotit} object holding graph data, or a bare
\code{plotit_graph}.  The graph must be acyclic.}

\item{node_width}{Horizontal width of node rectangles (unit-square
fraction).}

\item{padding}{Vertical gap between nodes in the same layer.}

\item{curvature}{Ribbon curvature in \verb{[0, 1]}; \code{0.5} gives symmetric
horizontal-tangent beziers.}

\item{n_points}{Samples per ribbon boundary curve.}

\item{max_sweeps}{Barycenter refinement sweeps (deterministic).}
}
\value{
A modified \code{plotit} object (pipeline form), or a new
\code{plotit_graph} with a \code{ribbons} table when called on raw graph data.
}
\description{
Layered flow layout for DAG edge tables: nodes become rectangles
(\code{xmin}/\code{xmax}/\code{ymin}/\code{ymax}, plus \code{xc}/\code{yc} centers), and each edge
becomes a closed bezier ribbon emitted to a third table named
\code{ribbons} in long-form polygon coordinates (\code{x}, \code{y}, \code{.ribbon_id}
plus all original edge attribute columns for fill mapping).
}
\details{
The layout is fully deterministic: layers follow longest-path depths,
refined by barycenter sweeps; no seed is required.
}
\examples{
e <- data.frame(
  source = c("A", "A", "B", "B", "C"),
  target = c("B", "C", "C", "D", "D"),
  value  = c(10, 5, 8, 3, 6)
)
g <- as_graph(e) |> layout_sankey()
names(g)
head(g$nodes[, c("id", "xmin", "ymin")])

as_graph(e) |>
  plotit() |>
  layout_sankey() |>
  mark_polygon(
    data = ~ribbons,
    encode(fill = source, group = .ribbon_id),
    alpha = 0.5
  ) |>
  mark_rect(data = ~nodes, encode(fill = id))
}
