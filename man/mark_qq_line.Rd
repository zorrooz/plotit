% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_qq_line}
\alias{mark_qq_line}
\title{Quantile-quantile reference line layer}
\usage{
mark_qq_line(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  distribution = "norm",
  line.p = c(0.25, 0.75),
  fullrange = FALSE,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics (inherit from the QQ layer's
data: \code{x} required)}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_qq_line}}

\item{distribution}{Theoretical quantile function name without \code{q}
(default \code{"norm"}).}

\item{line.p}{Quantile pair used for the fit (default \code{c(0.25, 0.75)}).}

\item{fullrange}{If \code{TRUE}, extend the line across the panel range.}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Adds a fitted reference line to a \code{\link[=mark_qq]{mark_qq()}} layer: quantiles of the
data projected onto the theoretical distribution.  A two-parameter fit
passes through the first and third quartile pairs.
}
\examples{
ecdf_data <- data.frame(eruptions = faithful$eruptions)
plotit(ecdf_data, encode(x = eruptions)) |>
  mark_qq() |>
  mark_qq_line()
}
\references{
R: \code{stats::qqline()} (quartile-pair reference line)
}
