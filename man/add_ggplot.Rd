% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{add_ggplot}
\alias{add_ggplot}
\title{Add a ggplot2 component to a plot}
\usage{
add_ggplot(plot, component)
}
\arguments{
\item{plot}{A \code{plotit} object (or a \code{plotit_composite}).}

\item{component}{Any object ggplot2's \code{+} accepts (layer, scale, coord,
facet, theme, labs, or a ggplot2 object).}
}
\value{
A modified \code{plotit} (or \code{plotit_composite}) object.
}
\description{
Escape hatch for advanced ggplot2 usage: \code{add_ggplot(p, ggplot2::annotate(...))}, \code{add_ggplot(p, ggplot2::guides(...))},
\code{add_ggplot(p, ggplot2::labs(...))} etc. modify the underlying ggplot
and return the \code{plotit} object so the pipeline continues.  Prefer the
verb API (\verb{mark_*}, \verb{scale_*}, \verb{label_*}, \code{style}) for reproducible,
well-validated plots.
}
\examples{
p <- plotit(iris, encode(x = Sepal.Width, y = Sepal.Length)) |>
  mark_point() |>
  add_ggplot(ggplot2::annotate("text", x = 2.5, y = 7.9, label = "high", size = 3))
}
