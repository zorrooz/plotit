% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_curve}
\alias{mark_curve}
\title{Curved link layer}
\usage{
mark_curve(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  curvature = 0.5,
  angle = 90,
  arrow = NULL,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics (must include \code{x}, \code{y},
\code{xend}, \code{yend}; layout tables bind them automatically)}

\item{data}{Optional data for this layer; accepts a \code{~table} graph
reference}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_curve}}

\item{curvature}{Amount of curvature: \code{1} is a semicircle, smaller
values are flatter, negative values bend the other way (default 0.5).}

\item{angle}{Angle at which the curve approaches the endpoint, in
degrees (default 90).}

\item{arrow}{Optional \code{grid::arrow()} object to draw arrow heads.}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Draws curved segments between \verb{(x, y)} and \verb{(xend, yend)} endpoints --
the link/diagram edge for arc diagrams, bipartite layouts and network
charts, where straight rules overlap node labels.  Arrows are available
through \code{arrow = grid::arrow()} via \code{...}.
}
\examples{
edges <- data.frame(
  x = c(1, 2, 3), y = c(0, 0, 0),
  xend = c(2, 3, 4), yend = c(1, 1, 1)
)
plotit(edges, encode(x = x, y = y, xend = xend, yend = yend)) |>
  mark_point(colour = "#4E79A7", size = 3) |>
  mark_point(
    mapping = encode(x = xend, y = yend),
    colour = "#E15759", size = 3
  ) |>
  mark_curve(curvature = 0.3)

# curved flow with arrows
plotit(edges, encode(x = x, y = y, xend = xend, yend = yend)) |>
  mark_curve(arrow = grid::arrow(length = grid::unit(0.1, "cm")))
}
\references{
AntV G2: \href{https://g2.antv.antgroup.com/en/api/mark/link}{Link}

D3: \code{d3.linkHorizontal} / arc-diagram link generator
}
