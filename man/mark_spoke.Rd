% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_spoke}
\alias{mark_spoke}
\title{Spoke layer}
\usage{
mark_spoke(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics (must include \code{x}, \code{y},
\code{angle} in radians, and \code{radius})}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_spoke}}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Draws a radial segment ("spoke") from each point \verb{(x, y)} at \code{angle}
(radians) for \code{radius} length.  A first-class primitive for
direction/velocity fields and for radial network edges whose endpoints
are naturally expressed in polar terms.
}
\examples{
df <- data.frame(
  x = c(0, 1, 2), y = c(0, 1, 0),
  angle = c(0, pi / 2, pi), radius = c(0.5, 0.8, 0.3)
)
plotit(df, encode(x = x, y = y, angle = angle, radius = radius)) |>
  mark_spoke()
}
\references{
Vega-Lite: \href{https://vega.github.io/vega-lite/docs/spoke.html}{Spoke}
}
