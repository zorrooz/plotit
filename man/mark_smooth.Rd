% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_smooth}
\alias{mark_smooth}
\title{Smoothed conditional mean layer}
\usage{
mark_smooth(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  method = NULL,
  formula = NULL,
  se = NULL,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_smooth}}

\item{method}{Smoothing method: \code{"auto"} (loess for n<1000, gam otherwise),
\code{"lm"}, \code{"glm"}, \code{"gam"}, or \code{"loess"}.}

\item{formula}{Formula to use in the smoothing function.}

\item{se}{If \code{TRUE} (default), display confidence interval around smooth.}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Adds a smoothed conditional mean line with a confidence band.
Aids the eye in seeing patterns in the presence of overplotting.
}
\examples{
plotit(mtcars, encode(x = wt, y = mpg)) |> mark_smooth()
}
\references{
R: \code{stats::loess()} / \code{stats::lm()} (default smoothing methods)
Vega-Lite: achieved via \code{layer(point) + layer(line) + transform(regression)}

AntV G2: achieved via transform pipeline
}
