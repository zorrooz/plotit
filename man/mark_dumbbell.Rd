% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_dumbbell}
\alias{mark_dumbbell}
\title{Dumbbell comparison chart layer}
\usage{
mark_dumbbell(
  plot,
  mapping = NULL,
  data = NULL,
  colour_start = "#4E79A7",
  colour_end = "#E15759",
  line_colour = "grey50",
  point_size = 3,
  line_width = 1,
  ...
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics}

\item{data}{Optional data for this layer}

\item{colour_start}{Colour for the start point (default \code{"#4E79A7"}).}

\item{colour_end}{Colour for the end point (default \code{"#E15759"}).}

\item{line_colour}{Colour for the connecting line (default \code{"grey50"}).}

\item{point_size}{Size for both dumbbell points (default 3).}

\item{line_width}{Width for the connecting line (default 1).}

\item{...}{Other arguments passed to \code{mark_point()} calls}
}
\value{
Modified plotit object
}
\description{
Creates a dumbbell chart with two connected points showing before/after
or paired comparisons. This is a \strong{syntax-sugar composite mark}
combining two \code{mark_point} calls and a \code{geom_segment}.
}
\details{
Equivalent expansion:
\preformatted{
  p |> mark_rule(x = x, xend = x, y = y_start, yend = y_end) |>
       mark_point(x = x, y = y_start, colour = colour_start) |>
       mark_point(x = x, y = y_end, colour = colour_end)
}
}
\examples{
df <- data.frame(
  cat = LETTERS[1:5], before = c(3, 5, 2, 8, 4),
  after = c(7, 6, 5, 10, 6)
)
plotit(df, encode(x = cat, y = before, yend = after)) |>
  mark_dumbbell()
}
