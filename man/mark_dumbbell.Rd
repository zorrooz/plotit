% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_dumbbell}
\alias{mark_dumbbell}
\title{Dumbbell comparison chart layer}
\usage{
mark_dumbbell(
  plot,
  mapping = NULL,
  data = NULL,
  color_start = NULL,
  color_end = NULL,
  line_color = NULL,
  point_size = ._MARK_STYLE$point_head,
  line_width = ._MARK_STYLE$lw_thin,
  ...
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics}

\item{data}{Optional data for this layer}

\item{color_start}{Colour for the start point.
\code{NULL} (default) uses primary \verb{#0072B2} when no colour channel is
mapped; a live \code{colour} aesthetic colours both endpoints instead.}

\item{color_end}{Colour for the end point.
\code{NULL} (default) uses secondary \verb{#E15759} when no colour channel is
mapped; a live \code{colour} aesthetic colours both endpoints instead.}

\item{line_color}{Colour for the connecting line.
\code{NULL} (default) follows a mapped colour, else soft grey.}

\item{point_size}{Size for both dumbbell points (default 3).}

\item{line_width}{Width for the connecting line (default 0.5, connector rung).}

\item{...}{Other arguments passed to \code{mark_point()} calls}
}
\value{
Modified plotit object
}
\description{
Creates a dumbbell chart with two connected points showing before/after
or paired comparisons. This is a \strong{syntax-sugar composite mark}
combining two \code{mark_point} calls and a \code{geom_segment}.
}
\details{
Equivalent expansion:
\preformatted{
  p |> mark_rule(x = x, xend = x, y = y_start, yend = y_end) |>
       mark_point(x = x, y = y_start, colour = color_start) |>
       mark_point(x = x, y = y_end, colour = color_end)
}
}
\examples{
df <- data.frame(
  cat = LETTERS[1:5], before = c(3, 5, 2, 8, 4),
  after = c(7, 6, 5, 10, 6)
)
plotit(df, encode(x = cat, y = before, yend = after)) |>
  mark_dumbbell()
}
\references{
AntV G2: \href{https://g2.antv.antgroup.com/en/api/mark/link}{Link} (corelib)
}
