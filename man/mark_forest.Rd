% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_forest}
\alias{mark_forest}
\title{Forest plot layer (estimate + interval)}
\usage{
mark_forest(
  plot,
  mapping = NULL,
  data = NULL,
  ref = NULL,
  point_size = 2,
  bar_width = 0.4,
  line_color = ._MARK_STYLE$soft,
  line_width = ._MARK_STYLE$lw_thin,
  ...
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics (must include \code{x}, \code{y}, \code{xmin},
\code{xmax})}

\item{data}{Optional data for this layer}

\item{ref}{Reference value for the null-effect rule (e.g. \code{0} for
differences, \code{1} for ratios).  \code{NULL} (default) draws no rule.}

\item{point_size}{Size of the estimate points (default 2).}

\item{bar_width}{Width of the interval bars as a fraction of the
categorical slot (default 0.4).}

\item{line_color}{Colour for the interval bars and reference rule
(default \code{._MARK_STYLE$soft} = \code{"grey50"}).}

\item{line_width}{Stroke width for interval bars (default 0.5).}

\item{...}{Other arguments passed to the estimate point layer}
}
\value{
Modified plotit object
}
\description{
Draws an estimate point with its confidence interval per row -- the
standard meta-analysis / effect-size panel.  This is a \strong{syntax-sugar
composite mark} combining \code{\link[=mark_errorbar]{mark_errorbar()}} and \code{\link[=mark_point]{mark_point()}}, plus a
vertical reference rule when \code{ref} is supplied.
}
\details{
Equivalent expansion:
\preformatted{
  p |> mark_errorbar(width = 0.3) |>
       mark_point(size = 2) |>
       mark_rule(xintercept = ref, linetype = "dashed")
}

Each row needs \code{y} (the study/category label position), \code{x} (the
estimate) and \code{xmin}/\code{xmax} (the interval); map them through
\code{encode()}.  This is the standard horizontal forest; for a vertical
forest, flip the whole plot afterwards with
\code{project_cartesian(flip = TRUE)}.
}
\examples{
studies <- data.frame(
  trial = paste0("Trial ", 1:5),
  es    = c(0.42, 0.31, 0.55, 0.20, 0.48),
  lo    = c(0.10, -0.05, 0.30, -0.10, 0.22),
  hi    = c(0.74, 0.67, 0.80, 0.50, 0.74)
)
studies |>
  plotit(encode(x = es, y = trial, xmin = lo, xmax = hi)) |>
  mark_forest(ref = 0) |>
  project_cartesian(flip = TRUE)
}
\references{
tidyplots: \code{add_ci95_errorbar()} + \code{add_mean_dot()} + \code{add_reference_lines()}

Vega-Lite: \code{point} + \code{errorbar} layer composition
}
