% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{layout_dendrogram}
\alias{layout_dendrogram}
\title{Dendrogram layout}
\usage{
layout_dendrogram(plot, direction = c("down", "up", "left", "right"))
}
\arguments{
\item{plot}{A \code{plotit} object holding graph data, or a bare
\code{plotit_graph}.}

\item{direction}{Direction the tree grows: \code{"down"} (root on top),
\code{"up"}, \code{"right"}, or \code{"left"}.}
}
\value{
A modified \code{plotit} object (pipeline form), or a new
\code{plotit_graph} when called on raw graph data.
}
\description{
Positions a merge tree (from \code{\link[=as_graph]{as_graph()}} applied to an \code{hclust} or
\code{dendrogram} object) using node heights as the depth axis.  Leaf order
follows the original merge sides, so label ordering matches the cluster
analysis output.
}
\examples{
hc <- hclust(dist(USArrests[1:6, ]))
g <- as_graph(hc) |> layout_dendrogram()
head(g$nodes)

as_graph(hc) |>
  plotit() |>
  layout_dendrogram(direction = "down") |>
  mark_rule(data = ~edges) |>
  mark_point(data = ~nodes)
}
