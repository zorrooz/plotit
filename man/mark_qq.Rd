% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_qq}
\alias{mark_qq}
\title{Quantile-quantile points layer}
\usage{
mark_qq(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  distribution = "norm",
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics (\code{x} required, \code{y} optional)}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_qq} (e.g. \code{dparams})}

\item{distribution}{Theoretical distribution function without the
\code{q} prefix (\code{"norm"} default); any \verb{q*} function works: \code{"norm"},
\code{"unif"}, \code{"exp"}, \code{"lnorm"}, ...}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Adds sample quantiles against theoretical (or another sample's)
quantiles -- the classic normality check.  Map \code{x} to the sample; \code{y}
is optional (another sample for two-sample QQ).  Add a reference line
with \code{\link[=mark_qq_line]{mark_qq_line()}}.
}
\examples{
ecdf_data <- data.frame(eruptions = faithful$eruptions)
plotit(ecdf_data, encode(x = eruptions)) |>
  mark_qq() |>
  mark_qq_line()

# two-sample QQ against an exponential reference
set.seed(42)
df <- data.frame(value = rexp(200))
plotit(df, encode(x = value)) |>
  mark_qq(distribution = "exp") |>
  mark_qq_line(distribution = "exp")
}
\references{
Observable Plot: \href{https://observablehq.com/plot/plots/qq}{Plot.qq}
}
