% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_count}
\alias{mark_count}
\title{Count layer (overlap-aware points)}
\usage{
mark_count(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_count}}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Draws each unique point once, sized by the number of observations at
that location (\code{stat_sum}).  The standard answer to overplotting in
scatter plots of discrete or binned data; pair with \code{\link[=scale_radius]{scale_radius()}}
for an area-proportional legend.
}
\examples{
plotit(diamonds, encode(x = cut, y = carat)) |> mark_count()

plotit(diamonds, encode(x = carat, y = price)) |> mark_count()
}
\references{
Vega-Lite: \href{https://vega.github.io/vega-lite/docs/point.html}{Point} with \code{aggregate: count}

tidyplots: \code{add_count_dot()} equivalent
}
