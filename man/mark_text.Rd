% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_text}
\alias{mark_text}
\title{Text layer}
\usage{
mark_text(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  repel = FALSE,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics (e.g. \code{encode(label = ...)})}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_text} or \code{geom_text_repel}}

\item{repel}{If \code{TRUE}, use \code{ggrepel::geom_text_repel} instead of
\code{geom_text}. Requires the \pkg{ggrepel} package.}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Adds a text label layer. For automatic label placement with collision
avoidance, install the optional \pkg{ggrepel} package and set
\code{repel = TRUE}.
}
\examples{
plotit(mtcars, encode(x = wt, y = mpg, label = rownames(mtcars))) |>
  mark_text(size = 3)
}
