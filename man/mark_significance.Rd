% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_significance}
\alias{mark_significance}
\title{Significance annotation layer}
\usage{
mark_significance(
  plot,
  comparisons,
  y_position = NULL,
  y_offset = NULL,
  line_color = ._MARK_STYLE$ink,
  line_width = ._MARK_STYLE$lw_thin,
  text_size = ._MARK_STYLE$txt_note,
  tip_length = 0.02,
  ...
)
}
\arguments{
\item{plot}{A plotit object}

\item{comparisons}{A data frame with columns: \code{group1}, \code{group2},
\code{label}, and optionally \code{y_position}. Character columns are
matched against the x-axis variable.}

\item{y_position}{Numeric vector of y-positions for the brackets.
If omitted, auto-computed from data range.}

\item{y_offset}{Text offset above the bracket line (default 0.5).
In data units.}

\item{line_color}{Colour for the bracket lines
(default \code{._MARK_STYLE$ink} = \code{"grey30"}).}

\item{line_width}{Width of bracket lines (default 0.5).}

\item{text_size}{Size of significance label text (default 3.2).}

\item{tip_length}{Length of bracket end-tick lines (default 0.02).  Units
follow the axis type: a fraction of the level count on discrete x axes,
a fraction of the bracket's numeric span on continuous axes.}

\item{...}{Additional arguments passed to the label annotation
(\code{ggplot2::annotate("text", ...)}).}
}
\value{
Modified plotit object
}
\description{
Adds statistical significance brackets and labels between groups.
This is a \strong{syntax-sugar composite mark} that combines
\code{mark_rule} and \code{mark_text} internally.
}
\details{
Equivalent expansion:
\preformatted{
  p |> mark_rule(x = comp$group1, xend = comp$group2,
                 y = comp$y_position, yend = comp$y_position) |>
       mark_text(x = midpoint, y = comp$y_position + y_offset,
                 label = comp$label)
}
}
\examples{
df <- data.frame(group = c("A", "B", "C"), value = c(5, 8, 4))
comp <- data.frame(
  group1 = c("A", "A"), group2 = c("B", "C"),
  label = c("**", "ns")
)
plotit(df, encode(x = group, y = value)) |>
  mark_bar() |>
  mark_significance(comp, y_position = c(9, 6))
}
