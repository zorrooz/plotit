% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_hex}
\alias{mark_hex}
\title{Hexagonal heatmap layer}
\usage{
mark_hex(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  bins = NULL,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_hex}}

\item{bins}{Number of bins along both axes (default 30).}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Divides the x-y plane into hexagonal bins and fills each by the
count (or other aggregation) of observations in that bin.
Ideal for visualizing overplotting in large datasets.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plotit(ggplot2::diamonds[sample(nrow(ggplot2::diamonds), 1000), ],
         encode(x = carat, y = price)) |> mark_hex(bins = 20)
}
}
\references{
AntV G2: \href{https://g2.antv.antgroup.com/en/api/mark/heatmap}{Heatmap} (corelib)
}
