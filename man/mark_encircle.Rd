% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_encircle}
\alias{mark_encircle}
\title{Group annotation envelope layer}
\usage{
mark_encircle(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  shape = c("hull", "ellipse"),
  expand = 0.05,
  radius = 0.05,
  alpha = NULL,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional aesthetics: \code{x}, \code{y} plus an optional discrete
channel (\code{colour}/\code{fill}/\code{group}) giving one envelope per level}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to the underlying \code{geom_polygon}
(\code{fill}, \code{colour}, \code{linewidth} default to the primary token, the
faint neutral, and the thin stroke).}

\item{shape}{\code{"hull"} (convex hull) or \code{"ellipse"} (t-based data
ellipse, \code{stat_ellipse} engine at 0.95 level)}

\item{expand}{Proportional outward dilation of the envelope (default
0.05 = 5\% of each vertex's distance from the envelope centroid;
approximates ggforce's npc expansion).  \code{0} keeps the raw hull.}

\item{radius}{Corner-rounding strength (default 0.05): one Chaikin
round per 0.05 smooths the hull corners; \code{0} keeps sharp corners.
Simplified counterpart of ggforce's \code{concavity}.}

\item{alpha}{Envelope fill opacity; \code{NULL} (default) uses the
annotation token \code{alpha_annot} (0.18).}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Draws a smooth envelope around the clusters of a scatter plot, one per
group: the annotation sibling of \code{\link[=mark_point]{mark_point()}} (ggforce's
\code{geom_mark_hull} / \code{geom_mark_ellipse} in simplified, dependency-free
form).  This is a \strong{syntax-sugar composite mark}.
}
\details{
Equivalent expansion:
\preformatted{
  shape = "hull"    is  grDevices::chull() vertices per group, dilated
                    outward by `expand`, rounded by `radius`, then
                    mark_polygon(fill = primary, colour = faint)
  shape = "ellipse" is  ggplot2::stat_ellipse() per group, dilated the
                    same way, then mark_polygon()
}

ggforce's label and arrow decorations are not part of this layer;
annotate groups with \code{\link[=mark_text]{mark_text()}} / \code{\link[=mark_label]{mark_label()}} instead.
}
\examples{
plotit(iris, encode(x = Petal.Length, y = Petal.Width, colour = Species)) |>
  mark_point() |>
  mark_encircle(shape = "ellipse")
}
\references{
ggforce: \href{https://ggforce.data-imaginist.com/reference/geom_mark_hull.html}{geom_mark_hull} /
\href{https://ggforce.data-imaginist.com/reference/geom_mark_ellipse.html}{geom_mark_ellipse}

R: \code{grDevices::chull()} (convex hull) / ggplot2 \code{stat_ellipse()}
}
