% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{layout_tree}
\alias{layout_tree}
\title{Tree layout}
\usage{
layout_tree(plot, direction = c("down", "up", "left", "right"))
}
\arguments{
\item{plot}{A \code{plotit} object holding graph data (created via
\code{\link[=as_graph]{as_graph()}} + \code{\link[=plotit]{plotit()}}), or a bare \code{plotit_graph}.}

\item{direction}{Direction the tree grows: \code{"down"} (root on top),
\code{"up"}, \code{"right"}, or \code{"left"}.}
}
\value{
A modified \code{plotit} object (pipeline form), or a new
\code{plotit_graph} when called on raw graph data.
}
\description{
Arranges a rooted hierarchy with \link[igraph:layout_as_tree]{igraph::layout_as_tree}.  Edges must
point from parent to child; multiple roots (forests) are supported.
}
\examples{
\dontshow{if (requireNamespace("igraph", quietly = TRUE)) withAutoprint(\{ # examplesIf}
hc <- hclust(dist(USArrests[, 1:3]))
as_graph(hc) |>
  plotit() |>
  layout_tree(direction = "down") |>
  mark_rule(data = ~edges) |>
  mark_point(data = ~nodes)
\dontshow{\}) # examplesIf}
}
