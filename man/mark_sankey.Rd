% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_sankey}
\alias{mark_sankey}
\title{Sankey flow diagram layer}
\usage{
mark_sankey(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  node_colour = "grey30",
  flow_alpha = 0.5,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics. The default expects
\code{x}, \code{next_x}, \code{node}, \code{next_node}, and optionally \code{value}.}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment (rarely used for Sankey).}

\item{...}{Other arguments passed to \code{geom_sankey}}

\item{node_colour}{Colour for node rectangles (default \code{"grey30"}).}

\item{flow_alpha}{Alpha transparency for flow ribbons (default 0.5).}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Creates a Sankey diagram showing directed flows between nodes.
Requires the \pkg{ggsankey} package. Data should contain
\code{x}, \code{next_x}, \code{node}, and \code{next_node} columns as generated by
\code{ggsankey::make_long}.
}
\examples{
\dontrun{
if (requireNamespace("ggsankey", quietly = TRUE)) {
  df <- ggsankey::make_long(ggplot2::diamonds, cut, color)
  plotit(df, encode(x = x, next_x = next_x, node = node,
                    next_node = next_node, value = value)) |>
    mark_sankey()
}
}
}
\references{
AntV G2: \href{https://g2.antv.antgroup.com/en/api/mark/sankey}{Sankey} (graphlib)
}
