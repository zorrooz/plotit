% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{layout_chord}
\alias{layout_chord}
\title{Chord layout}
\usage{
layout_chord(
  plot,
  inner_radius = 0.65,
  pad_angle = 0.03,
  n_points = 60,
  curvature = 0.35,
  order_by = c("total", "appearance")
)
}
\arguments{
\item{plot}{A \code{plotit} object holding graph data, or a bare
\code{plotit_graph}.}

\item{inner_radius}{Inner radius of the ring, \verb{(0, 1)}.}

\item{pad_angle}{Gap between sectors in radians.}

\item{n_points}{Samples per boundary chain of each polygon.}

\item{curvature}{Inward bowing of bands, \verb{[0, 0.95]}.}

\item{order_by}{\code{"total"} sorts sectors by descending flow;
\code{"appearance"} keeps first-appearance order.}
}
\value{
A modified \code{plotit} object (pipeline form), or a new
\code{plotit_graph} with \code{arcs}/\code{ribbons} tables when called on raw graph
data.
}
\description{
Circular chord layout: nodes become annular sectors on a ring whose
angular spans are proportional to total incident flow; edges become
closed bezier bands crossing the interior.  Emits four tables:
}
\details{
\itemize{
\item \code{nodes} -- original attributes plus \code{flow_total}, arc angles
(\code{arc_lo}/\code{arc_hi}) and \code{xc}/\code{yc} label anchors outside the ring;
\item \code{edges} -- untouched topology rows;
\item \code{arcs} -- long-form sector polygons (\code{.arc_id}, \code{x}, \code{y}, node attrs)
rendered with \code{mark_polygon(data = ~arcs, group = .arc_id)};
\item \code{ribbons} -- long-form band polygons (\code{.ribbon_id}, \code{x}, \code{y}, edge
attrs) rendered with \code{mark_polygon(data = ~ribbons, group = .ribbon_id)}.
}

Duplicate \verb{(source, target)} pairs are summed into a single band.
Sectors are ordered by descending total flow by default; use
\code{order_by = "appearance"} to keep input order.  Fully deterministic.
}
\examples{
e <- data.frame(
  source = c("A", "A", "B", "B", "C"),
  target = c("B", "C", "C", "D", "D"),
  value  = c(10, 5, 8, 3, 6)
)
g <- as_graph(e) |> layout_chord()
g$nodes[, c("id", "flow_total")]

as_graph(e) |> plotit() |>
  layout_chord() |>
  mark_polygon(data = ~ribbons,
               encode(fill = source, group = .ribbon_id),
               alpha = 0.4) |>
  mark_polygon(data = ~arcs,
               encode(fill = id, group = .arc_id))
}
