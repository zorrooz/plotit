% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_label}
\alias{mark_label}
\title{Label layer}
\usage{
mark_label(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  repel = FALSE,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics (e.g. \code{encode(label = ...)})}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_label} or \code{geom_label_repel}}

\item{repel}{If \code{TRUE}, use \code{ggrepel::geom_label_repel} instead of
\code{geom_label}. Requires the \pkg{ggrepel} package.}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Adds a text layer where every label sits inside a rounded box -- the
readable-over-data sibling of \code{\link[=mark_text]{mark_text()}}.  For collision-avoiding
placement, install the optional \pkg{ggrepel} package and set
\code{repel = TRUE}.
}
\examples{
agg <- aggregate(mpg ~ cyl, data = mtcars, FUN = mean)
plotit(agg, encode(x = cyl, y = mpg, label = round(mpg, 1))) |>
  mark_point() |>
  mark_label(nudge_y = 1.5, size = 3)
}
\references{
AntV G2: \href{https://g2.antv.antgroup.com/en/api/mark/text}{Text} (\code{badge} state)
}
