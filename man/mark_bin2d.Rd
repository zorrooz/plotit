% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_bin2d}
\alias{mark_bin2d}
\title{2D binned heatmap layer}
\usage{
mark_bin2d(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  bins = NULL,
  binwidth = NULL,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_bin_2d}}

\item{bins}{Number of bins along each axis (default 30).}

\item{binwidth}{Bin width along each axis; overrides \code{bins} when given.}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Divides the x-y plane into rectangular bins and fills each by the count
(or another aggregation) of observations it holds.  The rectangular
sibling of \code{\link[=mark_hex]{mark_hex()}}: exact bin boundaries make counts easier to read
against the axes, hex bins pack denser.
}
\details{
The bin-count fill is owned by this closed statistical mark: it defaults
to the sequential viridis scale (colour-blind safe).  Chain
\code{\link[=scale_fill]{scale_fill()}} afterwards to replace it (last call wins).
}
\examples{
plotit(diamonds, encode(x = carat, y = price)) |>
  mark_bin2d(bins = 20)

plotit(faithful, encode(x = eruptions, y = waiting)) |>
  mark_bin2d(bins = 15) |>
  scale_fill(trans = "log")
}
\references{
Vega-Lite: \href{https://vega.github.io/vega-lite/docs/rect.html}{Rect} with \code{bin} transform

AntV G2: \href{https://g2.antv.antgroup.com/en/api/mark/heatmap}{Heatmap} (corelib)
}
