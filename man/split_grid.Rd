% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split.R
\name{split_grid}
\alias{split_grid}
\title{Generic for grid facets}
\usage{
split_grid(
  plot,
  ...,
  rows = NULL,
  cols = NULL,
  scales = "fixed",
  space = "fixed"
)
}
\arguments{
\item{plot}{A plotit object.}

\item{...}{Unnamed arguments are shorthand for \code{rows} (e.g. \code{Species}
becomes \code{rows = vars(Species)}). Named arguments (\code{labeller},
\code{switch}, ...) are passed through to \code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}}.}

\item{rows, cols}{Variables to facet by, wrapped in \code{ggplot2::vars()}.}

\item{scales}{Should scales be fixed ("fixed"), free ("free"), or free in
one dimension ("free_x", "free_y")?}

\item{space}{Should the space be fixed ("fixed"), free ("free"), or free in
one dimension ("free_x", "free_y")?}
}
\value{
A modified \code{plotit} object.
}
\description{
Generic for grid facets
}
\examples{
plotit(iris, encode(x = Sepal.Width, y = Sepal.Length)) |>
  mark_point() |>
  split_grid(rows = ggplot2::vars(Species)) |> get_plot()
}
