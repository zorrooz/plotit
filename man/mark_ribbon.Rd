% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_ribbon}
\alias{mark_ribbon}
\title{Statistical ribbon layer}
\usage{
mark_ribbon(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  stat = "identity",
  level = 0.95,
  ci_method = c("normal", "boot"),
  seed = NULL,
  alpha = NULL,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional aesthetics: \code{x} plus \code{ymin}/\code{ymax} for
\code{stat = "identity"}, or \code{x} (group) + \code{y} (value) for a statistical
entity.}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_ribbon}}

\item{stat}{Statistical entity: \code{"identity"} (pre-computed band) or
\code{"mean_sem"} / \code{"mean_sd"} / \code{"mean_range"} / \code{"mean_ci95"}
(per-group aggregation of \code{y}; shares the engine with
\code{\link[=mark_errorbar]{mark_errorbar()}}).}

\item{level}{Confidence level for \code{stat = "mean_ci95"}, in \verb{(0, 1)}.}

\item{ci_method}{\code{"normal"} (t-based approximation) or \code{"boot"}
(percentile bootstrap; requires \code{seed}).}

\item{seed}{RNG seed for \code{ci_method = "boot"}.}

\item{alpha}{Band fill opacity; \code{NULL} (default) uses the statistical
token \code{alpha_ci} (0.25), tuned so stacked evidence stays readable
behind points and lines.}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Draws an interval band.  This is a \strong{syntax-sugar composite mark} over
\code{geom_ribbon}:
}
\details{
Equivalent expansion:
\preformatted{
  stat = "identity"  is  mark_area()'s interval routing (ymin/ymax from
                     the data, no aggregation)
  stat = "mean_sem"  is  stat_summary(fun.data = mean_sem, geom = "ribbon")
                     (tidyplots add_sem_ribbon is the same shape)
}
}
\examples{
fit <- stats::loess(mpg ~ wt, data = mtcars)
band <- data.frame(
  wt = mtcars$wt,
  fit = stats::predict(fit),
  se = stats::predict(fit, se = TRUE)$se.fit
)
band$lo <- band$fit - 1.96 * band$se
band$hi <- band$fit + 1.96 * band$se
plotit(band, encode(x = wt, ymin = lo, ymax = hi)) |>
  mark_ribbon() |>
  mark_line(mapping = encode(x = wt, y = fit))

# statistical entity: mean +- sem per group from raw y
plotit(iris, encode(x = Species, y = Sepal.Length)) |>
  mark_ribbon(stat = "mean_sem") |>
  mark_point(stat = "summary", fun = "mean")
}
\references{
Vega-Lite: \href{https://vega.github.io/vega-lite/docs/errorband.html}{Errorband}
(\code{extent}: stderr <-> sem, stdev <-> sd, ci <-> ci95)

tidyplots: \code{add_sem_ribbon()} / \code{add_ci95()} (same aggregation shapes)
}
