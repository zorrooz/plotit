% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_violin}
\alias{mark_violin}
\title{Violin layer}
\usage{
mark_violin(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_violin}. Since ggplot2 4.0 the
quantile lines live on the stat: \verb{quantiles =} selects the quantiles
drawn and \code{quantile.colour}/\code{quantile.linetype}/\code{quantile.linewidth}
style them (\code{quantile.linetype = 0} hides them by default).}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Adds a violin plot layer showing the kernel density estimate of the data
at each position.
}
\examples{
plotit(iris, encode(x = Species, y = Sepal.Length)) |>
  mark_violin(quantiles = 0.5, quantile.linetype = "dashed")
}
\references{
AntV G2: \href{https://g2.antv.antgroup.com/en/api/general/shape}{density (violin)}
}
