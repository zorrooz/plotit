% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{+.plotit}
\alias{+.plotit}
\title{Add a ggplot2 component to a plotit object}
\usage{
\method{+}{plotit}(e1, e2)
}
\arguments{
\item{e1}{A \code{plotit} object.}

\item{e2}{Any object ggplot2's \code{+} accepts (layer, scale, coord, facet,
theme, labs, or a ggplot2 object).}
}
\value{
A modified \code{plotit} object.
}
\description{
Escape hatch for advanced ggplot2 usage: \code{p + ggplot2::annotate(...)},
\code{p + ggplot2::guides(...)}, \code{p + ggplot2::labs(...)} etc. modify the
underlying ggplot and return the \code{plotit} object so the pipeline
continues.  Prefer the verb API (\verb{mark_*}, \verb{scale_*}, \verb{label_*},
\code{style}) for reproducible, well-validated plots.
}
\examples{
plotit(iris, encode(x = Sepal.Width, y = Sepal.Length)) |>
  mark_point() +
  ggplot2::annotate("text", x = 2.5, y = 7.9, label = "high", size = 3)
}
