% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{layout_treemap}
\alias{layout_treemap}
\title{Treemap layout}
\usage{
layout_treemap(plot)
}
\arguments{
\item{plot}{A \code{plotit} object holding graph data, or a bare
\code{plotit_graph}.}
}
\value{
A modified \code{plotit} object (pipeline form), or a new
\code{plotit_graph} when called on raw graph data.
}
\description{
Recursive squarified tiling (Bruls et al.) of a value hierarchy.  Leaves
carry sizes in the node table's \code{value} column (build via
\code{as_graph()} on an \code{id}/\code{parent}/\code{value} hierarchy table); parent values
are aggregated from their descendants.  Every node gains
\code{xmin}/\code{xmax}/\code{ymin}/\code{ymax} within the unit square plus a \code{leaf} flag.
A derived \code{leaves} table is emitted for direct rendering with
\code{mark_rect(data = ~leaves)}.
}
\examples{
h <- data.frame(
  id     = c("root", "A", "B", "a1", "a2"),
  parent = c(NA, "root", "root", "A", "A"),
  value  = c(NA, NA, 50, 30, 20)
)
g <- as_graph(h) |> layout_treemap()
subset(g$nodes, leaf)[, c("id", "xmin", "xmax")]

as_graph(h) |>
  plotit() |>
  layout_treemap() |>
  mark_rect(data = ~leaves, encode(fill = id))
}
