% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_contour}
\alias{mark_contour}
\title{Contour layer for 2D scalar fields}
\usage{
mark_contour(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  filled = FALSE,
  bins = NULL,
  breaks = NULL,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics (must include \code{x}, \code{y}, \code{z})}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to the underlying geom}

\item{filled}{If \code{TRUE}, draw filled contour bands via
\code{geom_contour_filled}; otherwise contour lines via \code{geom_contour}.}

\item{bins}{Number of contour bins.}

\item{breaks}{Numeric vector of exact contour levels; overrides \code{bins}.}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Draws contour lines of a 2D scalar field: the data must carry a \code{z}
aesthetic (value at each \code{x}/\code{y} grid point).  Use \code{filled = TRUE} for
banded fills.  Where \code{\link[=mark_density_2d]{mark_density_2d()}} estimates density from points,
\code{mark_contour()} renders an \emph{observed} field (elevation, temperature,
a fitted surface).
}
\details{
Filled bands map \code{fill} to a computed level factor owned by this closed
statistical mark; the band scale defaults to discrete viridis and can be
replaced by chaining \code{\link[=scale_fill]{scale_fill()}} afterwards.
}
\examples{
df <- expand.grid(x = seq(0, 10, length.out = 30), y = seq(0, 10, length.out = 30))
df$z <- with(df, sin(x / 2) * cos(y / 2))
plotit(df, encode(x = x, y = y, z = z)) |>
  mark_contour(breaks = seq(-1, 1, by = 0.25))

plotit(df, encode(x = x, y = y, z = z)) |>
  mark_contour(filled = TRUE, bins = 10)
}
\references{
AntV G2: \href{https://g2.antv.antgroup.com/en/api/mark/contour}{Contour}

Observable Plot: \code{Plot.contour}
}
