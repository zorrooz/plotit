% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_step}
\alias{mark_step}
\title{Step layer}
\usage{
mark_step(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  direction = c("vh", "hv", "mid"),
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to \code{geom_step}}

\item{direction}{\code{"vh"} (default) draws vertical-then-horizontal steps;
\code{"hv"} the reverse; \code{"mid"} steps at the midpoint.}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Adds a stair-step line layer: observations are connected with
axis-parallel segments, so every change renders as an explicit jump.
Use for discrete state changes over time, reference thresholds, or
cumulative (ECDF-style) views.
}
\examples{
plotit(ggplot2::economics, encode(x = date, y = unemploy)) |>
  mark_step()

# horizontal-then-vertical steps
plotit(ggplot2::economics, encode(x = date, y = unemploy)) |>
  mark_step(direction = "hv")

# grouped steps
plotit(subset(ggplot2::economics, date > "1990-01-01"),
  encode(x = date, y = psavert, colour = "savings")
) |>
  mark_step() |>
  mark_line(colour = "#E15759", alpha = 0.3)
}
\references{
Vega-Lite: \href{https://vega.github.io/vega-lite/docs/line.html}{Line} with \code{interpolate: "step"}

AntV G2: \href{https://g2.antv.antgroup.com/en/api/mark/line}{Line} with \code{shape: "hv"}
}
