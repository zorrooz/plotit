% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{as_graph}
\alias{as_graph}
\title{Convert relational data to a plotit graph}
\usage{
as_graph(
  edges,
  nodes = NULL,
  source = "source",
  target = "target",
  value = NULL,
  directed = FALSE
)
}
\arguments{
\item{edges}{Primary relational input.  One of:
\itemize{
\item a data.frame edge list (columns selected by \code{source}/\code{target}/\code{value});
\item a matrix or \code{xtabs}/table of flows (melted automatically);
\item an \code{hclust} or \code{dendrogram} object;
\item a \code{tbl_graph}.
}}

\item{nodes}{Optional node attribute table.  Must contain a column named
\code{id}.  When omitted, nodes are generated implicitly from the edge
endpoints in first-appearance order.}

\item{source, target}{Column names (bare or quoted) locating the edge
endpoints in \code{edges}.  Defaults \code{"source"} / \code{"target"}.}

\item{value}{Column name (bare or quoted) holding flow magnitudes.
Defaults to a column named \code{"value"} when present, otherwise unit
weights are used.  The output column is always named \code{value}.}

\item{directed}{Logical; whether the relation is directed.}
}
\value{
An S3 \code{plotit_graph} object: a named list with at least \code{nodes}
(\code{id} column first) and \code{edges} (\code{source}, \code{target}, \code{value}).
}
\description{
Coerces edge lists, adjacency matrices, contingency tables, cluster trees,
or \code{tbl_graph} objects into a \code{plotit_graph}: a named list of data frames
(\code{nodes}, \code{edges}) that can initialize \code{\link[=plotit]{plotit()}} and be positioned by
\verb{layout_*()} transforms.  Layout coordinates are never mapped by hand --
they are produced by layouts and bound automatically at mark time.
}
\examples{
e <- data.frame(source = c("a", "b"), target = c("b", "c"))
g <- as_graph(e)
names(g)
g$edges
}
