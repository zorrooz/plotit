% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compose.R
\name{compose_marginal}
\alias{compose_marginal}
\title{Scatter plot with marginal distributions}
\usage{
compose_marginal(
  main,
  top,
  right,
  widths = c(4, 1),
  heights = c(1, 4),
  guides = "collect"
)
}
\arguments{
\item{main}{A \code{plotit} scatter plot (must have both x and y mapped).}

\item{top}{A \code{plotit} histogram or density plot for the x variable.
Typically built from the same data and x mapping as \code{main}, with the
same \code{fill}/\code{colour} aesthetic to match.}

\item{right}{A \code{plotit} histogram or density plot for the y variable.
Same conventions as \code{top}.  Call \code{project_cartesian(flip = TRUE)} on
this plot before passing it so the y-axis aligns with the scatter.}

\item{widths}{Relative column widths for the main and right-marginal
panels.  Default \code{c(4, 1)} = right marginal is 1/5 of total width.}

\item{heights}{Relative row heights for the top-marginal and main
panels.  Default \code{c(1, 4)} = top marginal is 1/5 of total height.}

\item{guides}{\code{"collect"} (default) to merge legends across all panels,
\code{"keep"} to keep them separate, \code{NULL} for patchwork auto-detect.}
}
\value{
A \code{plotit_composite} object.  Pipe to \code{label_title()},
\code{style()}, \code{export()} as usual.
}
\description{
Arranges a main scatter plot with marginal histogram or density plots on
the top (x-axis distribution) and right (y-axis distribution).  The axes
are shared so that the marginal bins align exactly with the scatter axes.
}
\examples{
main <- plotit(iris, encode(x = Sepal.Width, y = Sepal.Length, colour = Species)) |> mark_point()
top <- plotit(iris, encode(x = Sepal.Width, fill = Species)) |> mark_histogram(bins = 15, alpha = 0.5)
right <- plotit(iris, encode(x = Sepal.Length, fill = Species)) |>
  mark_histogram(bins = 15, alpha = 0.5) |>
  project_cartesian(flip = TRUE)
compose_marginal(main, top, right)
}
