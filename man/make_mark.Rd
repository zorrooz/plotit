% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/factory.R
\name{make_mark}
\alias{make_mark}
\title{Create a custom mark}
\usage{
make_mark(name, geom_fun)
}
\arguments{
\item{name}{Mark name as a string (e.g. \code{"mark_spoke"}).
Should start with \code{"mark_"}.}

\item{geom_fun}{A ggplot2 geom function
(e.g. \code{ggplot2::geom_spoke}).}
}
\value{
Invisibly returns the registered S7 generic.
}
\description{
Registers a new S7 generic + method from any ggplot2 geom function,
making it available in the plotit pipeline. The new mark behaves
identically to built-in marks: it supports \code{mapping}, \code{data},
\code{position}, auto-dodge, and rasterization.
}
\section{Stability}{

Extension surface (contract tier: extensible). The signature is stable as
documented; registration behaviour (how the new mark is exposed to the
pipeline) may be iterated in future releases without a deprecation
cycle. Marks registered via \code{make_mark()} that are not listed in
\code{._MARK_CHROME} keep \code{axis = "keep"} by default (zero behavioural
difference vs. hand-rolled layers).
}

\examples{
make_mark("mark_spoke", ggplot2::geom_spoke)
df <- data.frame(
  x = 1:5, y = 1:5,
  angle = seq(0, 2 * pi, length.out = 5),
  radius = rep(0.3, 5)
)
# Now usable in the pipeline:
df |>
  plotit(encode(x = x, y = y, angle = angle, radius = radius)) |>
  mark_spoke()
}
