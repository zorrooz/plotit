% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_lollipop}
\alias{mark_lollipop}
\title{Lollipop chart layer}
\usage{
mark_lollipop(
  plot,
  mapping = NULL,
  data = NULL,
  stem_color = ._MARK_STYLE$soft,
  stem_width = ._MARK_STYLE$lw_thin,
  point_size = ._MARK_STYLE$point_head,
  ref = 0,
  ...
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional new aesthetics}

\item{data}{Optional data for this layer}

\item{stem_color}{Colour for the stem lines
(default \code{._MARK_STYLE$soft} = \code{"grey50"}).}

\item{stem_width}{Line width for stems (default 0.5).}

\item{point_size}{Point size for the lollipop head (default 3).}

\item{ref}{Baseline value for the stems (default 0).}

\item{...}{Other arguments passed to \code{mark_point()}}
}
\value{
Modified plotit object
}
\description{
Creates a lollipop chart: a point anchored by a stem to a reference
line. This is a \strong{syntax-sugar composite mark} combining
\code{geom_segment} and \code{geom_point}.
}
\details{
Equivalent expansion:
\preformatted{
  p |> mark_rule(x = x, xend = x, y = ref, yend = y) |>
       mark_point(x = x, y = y)
}
}
\examples{
df <- data.frame(cat = LETTERS[1:5], val = c(3, 7, 2, 9, 5))
plotit(df, encode(x = cat, y = val)) |>
  mark_lollipop(point_size = 4, stem_color = "grey70")
}
