% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_heatmap}
\alias{mark_heatmap}
\title{Matrix heatmap layer (sugar)}
\usage{
mark_heatmap(
  plot,
  cluster = c("both", "row", "column", "none"),
  scale = c("none", "row", "column"),
  ...,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object carrying the matrix / data.frame / tidy mapping.}

\item{cluster}{Reorder axes by hierarchical clustering: \code{"both"} (default),
\code{"row"}, \code{"column"}, or \code{"none"}.}

\item{scale}{z-score normalisation: \code{"none"} (default), \code{"row"}, or
\code{"column"}.}

\item{...}{Other arguments passed to \code{\link[ggplot2:geom_tile]{ggplot2::geom_tile()}}.}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object.
}
\description{
Renders a tidyheatmaps-style matrix heatmap as a plotit mark. Accepts a
numeric matrix, a wide numeric data.frame (columns become heatmap
columns), or a tidy long mapping (\code{encode(x =, y =, fill =)}). Optionally
z-score normalises rows/columns (\code{\link[base:scale]{base::scale()}}) and reorders them by
hierarchical clustering (\code{\link[stats:hclust]{stats::hclust()}}). The tile grid reuses the
shared mark path, so it inherits the white-hairline cell chrome (tiles
hug the panel, no 0-origin whitespace) and the colour-blind-safe viridis
fill default; chain \code{\link[=scale_fill]{scale_fill()}} to replace it (last call wins). This is
a mark, not a separate system: combine it with \code{\link[=layout_dendrogram]{layout_dendrogram()}} and
\code{\link[=compose_marginal]{compose_marginal()}} for a full annotated heatmap.
}
\examples{
mat <- matrix(rnorm(30),
  nrow = 6,
  dimnames = list(paste0("g", 1:6), paste0("s", 1:5))
)
plotit(mat, encode()) |> mark_heatmap()
plotit(mat, encode()) |> mark_heatmap(cluster = "both", scale = "row")
}
\references{
tidyheatmaps: \href{https://jbengler.github.io/tidyheatmaps/}{Heatmaps from Tidy Data}
}
