% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{mark_rule}
\alias{mark_rule}
\title{Reference line / segment layer}
\usage{
mark_rule(
  plot,
  mapping = NULL,
  data = NULL,
  xintercept = NULL,
  yintercept = NULL,
  slope = NULL,
  intercept = NULL,
  x = NULL,
  xend = NULL,
  y = NULL,
  yend = NULL,
  colour = NULL,
  linetype = NULL,
  linewidth = NULL,
  ...,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional aesthetics for data-driven segments
(\code{x}/\code{xend}/\code{y}/\code{yend}); layout tables bind them automatically.}

\item{data}{Optional data for segment mode: one segment per row.
Accepts a data.frame or a \code{~table} reference into graph data.}

\item{xintercept}{x-intercept for vertical line(s)}

\item{yintercept}{y-intercept for horizontal line(s)}

\item{slope}{Line slope for abline}

\item{intercept}{Line intercept for abline}

\item{x}{Start x coordinate(s) for segment}

\item{xend}{End x coordinate(s) for segment}

\item{y}{Start y coordinate(s) for segment}

\item{yend}{End y coordinate(s) for segment}

\item{colour}{Line colour (default \code{"grey50"}, the unified soft neutral;
ggplot2's black is restored by passing \code{colour = "black"}).}

\item{linetype}{Line type}

\item{linewidth}{Line width in mm (default 0.5).}

\item{...}{Other arguments passed to the underlying geom}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Adds one or more reference lines or segments to a plot. Dispatches
to the appropriate ggplot2 geom based on the parameters supplied:
}
\details{
\itemize{
\item \code{xintercept} → \link[ggplot2:geom_abline]{ggplot2::geom_vline}
\item \code{yintercept} → \link[ggplot2:geom_abline]{ggplot2::geom_hline}
\item \code{slope} + \code{intercept} → \link[ggplot2:geom_abline]{ggplot2::geom_abline}
\item \code{x}/\code{xend}/\code{y}/\code{yend} → \link[ggplot2:geom_segment]{ggplot2::geom_segment}
}

Dispatch priority: vline/hline > abline > segment.
}
\examples{
plotit(iris, encode(x = Sepal.Width, y = Sepal.Length)) |>
  mark_rule(xintercept = 3, colour = "red", linetype = "dashed")

# Data-driven segments: network edges from a layout_* transform
if (requireNamespace("igraph", quietly = TRUE)) {
  e <- data.frame(source = c("a", "a", "b"), target = c("b", "c", "c"))
  as_graph(e) |>
    plotit() |>
    layout_force(seed = 1) |>
    mark_point(data = ~nodes) |>
    mark_rule(data = ~edges, colour = "grey70")
}
}
\references{
Vega-Lite: \href{https://vega.github.io/vega-lite/docs/rule.html}{Rule}

AntV G2: \href{https://g2.antv.antgroup.com/en/api/mark/line-x}{LineX} /
\href{https://g2.antv.antgroup.com/en/api/mark/line-y}{LineY} /
\href{https://g2.antv.antgroup.com/en/api/mark/range}{Range}
}
