% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark_image.R
\name{mark_image}
\alias{mark_image}
\title{Image layer}
\usage{
mark_image(
  plot,
  mapping = NULL,
  data = NULL,
  position = NULL,
  ...,
  size = 0.05,
  clip = "rectangle",
  interpolate = TRUE,
  rasterize = FALSE,
  rasterize_dpi = 300,
  rasterize_dev = "cairo"
)
}
\arguments{
\item{plot}{A plotit object}

\item{mapping}{Optional aesthetics: \code{x}, \code{y}, \code{src}}

\item{data}{Optional data for this layer}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed to the internal layer constructor.
Rotation (Observable Plot \code{rotate}) is a recorded extension position,
not implemented.}

\item{size}{Larger image dimension as a fraction of the panel
(default 0.05).}

\item{clip}{\code{"rectangle"} (default) or \code{"circle"} (crop to a disc).}

\item{interpolate}{Pass smooth interpolation to the raster (default
\code{TRUE}; \code{FALSE} keeps pixel edges crisp, useful for large-scale
isotype tiles).}

\item{rasterize}{If \code{TRUE}, rasterize via \code{ggrastr::rasterise()}.}

\item{rasterize_dpi}{DPI for rasterization (default 300).}

\item{rasterize_dev}{Graphics device for rasterization (default \code{"cairo"}).}
}
\value{
Modified plotit object
}
\description{
Places an image at each (x, y): the isotype / photo-scatter mark
(Observable Plot \code{Image}).  Sources are png/jpeg file paths, http(s)
URLs, or pre-read raster arrays (e.g. via \pkg{magick}); arrays skip
decoding entirely.  A single \code{size} sets the image's larger dimension
as a fraction of the panel and the aspect ratio is preserved
(\code{clip = "circle"} crops to a disc).
}
\details{
Rows with \code{NA} or empty \code{src} are dropped.  Images are decoded once per
source and cached for the session.  Like any glyph, an image that would
extend past the data range is clipped by the panel: boundary placements
should widen the scale limits (e.g. \code{scale_x(limits = c(0, 4))}).
}
\examples{
\dontshow{if (requireNamespace("png", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# build a small in-memory png, then scatter it
tmp_png <- tempfile(fileext = ".png")
png::writePNG(array(c(0.2, 0.4, 0.8), dim = c(1, 1, 3)), tmp_png)
df <- data.frame(x = c(1, 2), y = c(1, 2), src = tmp_png)
plotit(df, encode(x = x, y = y, src = src)) |>
  mark_image(size = 0.2)
\dontshow{\}) # examplesIf}
}
\references{
Observable Plot: \code{Plot.image} (src channel, round clip, imageRendering)

AntV G2: \href{https://g2.antv.antgroup.com/en/examples}{image} (via G2 mark extensions)
}
