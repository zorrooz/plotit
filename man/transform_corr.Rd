% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{transform_corr}
\alias{transform_corr}
\title{Correlation preprocessing transform}
\usage{
transform_corr(
  data,
  method = c("pearson", "spearman", "kendall"),
  reorder = TRUE
)
}
\arguments{
\item{data}{A data.frame with at least two numeric columns.}

\item{method}{Correlation method: \code{"pearson"} (default), \code{"spearman"},
or \code{"kendall"}.}

\item{reorder}{If \code{TRUE} (default), reorder rows and columns by
hierarchical clustering.  Skipped with a warning when the matrix
contains NA (e.g. zero-variance columns).}
}
\value{
A data.frame with columns \code{Var1}, \code{Var2} (factors) and
\code{value} (numeric correlation).
}
\description{
Computes pairwise correlations over the numeric columns of \code{data} and
melts the matrix into a long-form table (\code{Var1}, \code{Var2}, \code{value}),
optionally reordering rows/columns by hierarchical clustering.
\code{\link[=mark_corr]{mark_corr()}} is sugar over this transform plus a tile layer; call it
directly when you need the table for custom rendering or inspection.
}
\examples{
head(transform_corr(mtcars[, c("mpg", "disp", "hp")]))
}
