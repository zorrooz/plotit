% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{project_map}
\alias{project_map}
\title{Map coordinate system}
\usage{
project_map(
  plot,
  projection = NULL,
  xlim = NULL,
  ylim = NULL,
  clip = "on",
  ...
)
}
\arguments{
\item{plot}{A plotit object.}

\item{projection}{Map projection name (e.g. \code{"mercator"}, \code{"orthographic"}).
\code{NULL} uses \code{coord_sf()} default.}

\item{xlim, ylim}{Longitude/latitude limits. \code{NULL} = auto.}

\item{clip}{Should drawing be clipped? \code{"on"} or \code{"off"}.}

\item{...}{Passed to \code{coord_sf()} or \code{coord_map()}.}
}
\value{
Modified plotit object.
}
\description{
Applies a geographic projection. Uses \code{ggplot2::coord_sf()} by default
(for simple features), or \code{ggplot2::coord_map()} when a \code{projection}
string is provided (requires \pkg{mapproj}).
}
\examples{
\dontshow{if ((requireNamespace("sf", quietly = TRUE))) withAutoprint(\{ # examplesIf}
# requires the sf package
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
plotit(nc, encode()) |> project_map()
\dontshow{\}) # examplesIf}
}
