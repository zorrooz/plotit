% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compose.R
\name{compose_annot}
\alias{compose_annot}
\title{Attach annotation strips to a base plot}
\usage{
compose_annot(
  base,
  top = NULL,
  bottom = NULL,
  left = NULL,
  right = NULL,
  heights = NULL,
  widths = NULL,
  gap = 0,
  guides = "collect",
  align = "panel",
  on_top = FALSE
)
}
\arguments{
\item{base}{A \code{plotit} or \code{plotit_composite} object.}

\item{top, bottom, left, right}{Optional \code{plotit}/\code{plotit_composite}
strips attached to the matching side.}

\item{heights, widths}{Strip sizes for the vertical (top/bottom) and
horizontal (left/right) sides: \code{NULL} (default) = 0.9 inch per strip;
\code{grid::unit()} = absolute size; numeric = ratio relative to the base
row/column (same three-state contract as \code{\link[=compose_grid]{compose_grid()}}).}

\item{gap}{Space between the strips and the base plot in inches
(default 0; realised as empty spacer rows/columns).}

\item{guides}{\code{"collect"} (default) to merge legends, \code{"keep"} to keep
per-panel legends.}

\item{align}{Alignment reference. \code{"panel"} (default) = strips align to
the base panel area.  \code{"plot"} requests full-area alignment; grid
members always align on panel areas, so it currently falls back to
\code{"panel"} (documented approximation).}

\item{on_top}{Reserved for overlap semantics (use \code{\link[=compose_inset]{compose_inset()}});
\code{TRUE} warns and renders the strips beside the base.}
}
\value{
A \code{plotit_composite} object.
}
\description{
\code{compose_annot()} glues one to four \code{plotit} strips (dendrograms,
annotation bars, marginal densities) onto the matching sides of a base
plot.  Strips share the base panel span: patchwork aligns the panel
areas of plots stacked in the same design row/column, so tree tips and
annotation cells line up with the base heatmap rows/columns by
construction.
}
\details{
This is the same assembly engine behind \code{\link[=compose_marginal]{compose_marginal()}}
(\code{marginal} = a \code{top} + \code{right} special case with axis-hiding
conventions).  The result can be nested into \code{\link[=compose_grid]{compose_grid()}}.
}
\examples{
mat <- matrix(c(9, 1, 8, 2, 1, 9, 2, 8, 5, 3, 7, 4),
  nrow = 4,
  dimnames = list(paste0("g", 1:4), paste0("s", 1:3))
)
h <- stats::hclust(stats::dist(mat))
hm <- plotit(mat, encode()) |> mark_heatmap(cluster = h)
tree <- as_graph(h) |>
  plotit() |>
  layout_dendrogram(direction = "up") |>
  mark_rule(data = ~edges)
hm |> compose_annot(top = tree)
}
